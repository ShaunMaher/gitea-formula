# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: gitea
  rootgroup: root
  config: '/etc/gitea'
  service:
    name: gitea
  subcomponent:
    config: '/etc/gitea-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
